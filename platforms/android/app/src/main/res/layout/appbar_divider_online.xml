<?xml version="1.0" encoding="utf-8"?>
<layout>
    <LinearLayout android:id="@+id/toggle_divider"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp"
        android:orientation="vertical"
        app:layout_constraintBottom_toBottomOf="@id/target_player_toggle_button"
        app:layout_constraintEnd_toStartOf="@id/filter_container"
        app:layout_constraintHorizontal_weight="1.5"
        app:layout_constraintStart_toEndOf="@id/target_player_toggle_button"
        app:layout_constraintTop_toTopOf="@id/target_player_toggle_button"
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto">

        <View
            android:layout_width="2dp"
            android:layout_height="0dp"
            android:layout_gravity="center_horizontal"
            android:layout_weight="1"
            android:layout_marginBottom="6dp"
            android:background="@color/colorPrimaryLight" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="Online"
            android:textColor="@color/colorPrimaryLight" />
    </LinearLayout>
</layout>